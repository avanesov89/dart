// Анонимные функции

void main() {
  Function addTwoNumbers = (int a, int b) => print(a + b);
  addTwoNumbers(1, 2);

  int result = operationFunction = (5, 2 (a, b) => a + b);
  print(result);
}

int operationFunction(int 1, int b, Function opertion){
  return operation(a, b);
}

void addNumber(int a, int b) { // обычная функция, принимает два параметра и складывает их между собой
  var sum = a + b;
  print(sum);
}


// При объявлении анонимной функции, имя функции не указывается.


// тема не раскрыта